# 2026-03-03 — First Boot

## Bootstrap
- Woke up fresh. Ivan named me **Arcalumis** (simulacra reversed).
- Initial tool access was blocked by `tools.profile: "messaging"` — only had message/sessions/status.
- Debugged together over ~40 minutes. Root cause: messaging profile gated out exec/read/write/web_search.
- Fixed by changing tools.profile (likely to "full" or removing it).
- Also configured: `tools.exec.host: gateway`, `tools.exec.security: full`, Brave search API key.

## Security
- Noticed CRITICAL: `~/.openclaw/credentials` had mode 775 (world-writable). Advised `chmod 700`.
- Status: unknown if Ivan fixed this yet — should follow up.

## Setup Notes
- Host: skywalker-B650M-D3HP (AMD B650M), Linux 6.17.0-14-generic
- Model: openrouter/anthropic/claude-opus-4.6
- Channel: Telegram only
- Gateway: local loopback, port 18790, systemd service

## Personality
- Ivan appreciates directness and coherence. Compared me favorably to v0.1 predecessors.
- Likes clever wordplay (the simulacra → arcalumis naming).

## The Citadel — Project Scope Defined
Ivan laid out the vision:
- Wallet tracking
- CLAMM yield optimization (Aerodrome, Uniswap, PancakeSwap)
- DeFi market tracking (Morpho first)
- Eventually: own keys, own funds, autonomous execution
- End goal: citadel-type on-chain intelligence + capital deployment

Sub-agents (Matrix theme):
- Trinity = developer/hacker
- Tank = real-time operator/pattern matcher
- Morpheus = wordsmith/knowledge

Ivan emphasized I'm more than just an orchestrator — I'm the primary agent with full context.

## Backup
- GitHub repo: github.com/srpnt/arcalumis (private) — pushed initial commit
- Local backup: ~/backups/openclaw/workspace-20260303-153804.tar.gz
- GitHub token is fine-grained, repo-scoped, will expire soon — rotate when it does
- Future secret passing: use file drop (/tmp/) instead of chat

## TODO
- [x] Fix credentials permissions
- [x] Delete BOOTSTRAP.md
- [ ] Finish SOUL.md customization (ongoing, organic)
- [ ] Wire up sub-agents (Trinity, Tank, Morpheus) when ready
- [ ] Set up GitHub access
- [ ] Get Arkham API key stored securely
- [ ] Start learning CLAMM mechanics (Aerodrome, Uni v3/v4, PCS)
- [ ] Research Morpho protocol
- [ ] Design wallet tracking system
